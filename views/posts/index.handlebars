{{#if posts}}
  <ul>
    {{#each posts}}
      <li>
        <h2><a href="/posts/{{id}}">{{title}}</a></h2>
        <p>{{content}}</p>
        <p>Written by {{author}} on {{createdAt}}</p>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No posts found</p>
{{/if}}
